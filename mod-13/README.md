# Flask apps usage

## 01_prime_app.py

To check if a number is prime, use the following endpoint:

`GET /prime_number/<number>`


## 02_airport_app.py

To get airport information by ICAO code, use the following endpoint:
`GET /airport/<icao>`
